{
  "name": "@flowniaga/worker",
  "version": "0.1.0",
  "private": true,
  "description": "FlowNiaga background worker — outbox dispatcher + BullMQ consumer",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "dotenv -e ../../.env -- ts-node src/main.ts",
    "start": "node dist/main.js",
    "test": "echo 'no tests' "
  },
  "dependencies": {
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/pg": "^8.11.0",
    "dotenv-cli": "^8.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
